root_dir: .

file_roots:
  base:
    - srv/salt
    - srv/formulas/dnsmasq-formula

pillar_roots:
  base:
    - srv/pillar

ssh_log_file: var/log/salt/ssh

fileserver_backend:
  - roots

nodegroups:
  manager_group: 'm*'
